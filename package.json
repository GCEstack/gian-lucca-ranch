{
  "name": "gian-lucca-ranch",
  "version": "1.0.0",
  "description": "Gian Lucca's Ranch — a sunny ranch page built with love by Daddy",
  "private": true,
  "scripts": {
    "dev": "npx serve . -l 3000",
    "build": "echo 'Static site — no build step required'",
    "vercel-build": "npm run build",
    "start": "npx serve . -l 3000"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=18"
  }
}
